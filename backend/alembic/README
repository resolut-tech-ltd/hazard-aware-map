Generic single-database configuration with async support for Alembic migrations.

To create a new migration:
  alembic revision --autogenerate -m "Description of changes"

To apply migrations:
  alembic upgrade head

To rollback:
  alembic downgrade -1
